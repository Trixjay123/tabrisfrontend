<h1>Checklist for front-end<h1/>
<li>
    <ol>Add return to homepage button on image hoster site<ol/>
    <ol>Create view image page, contains return to homepage, download, and shows image metadata. At the top there should be comments on where to inject the image link<ol/>
    <ol>Fix the button on gallery page, the image hoster one<ol/>
    <ol>Create a gallery that will display all images, recieves JSON of all images hosted, need to find a way to display all those elements though<ol/>
    <ol>Make login page pretty<ol/>
    <ol>Fix the proportions of the search bar and where its located<ol/>
    <ol>placement of home button on gallery page<ol/>
<li/>